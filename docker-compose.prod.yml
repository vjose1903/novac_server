services:
  db-prod:
    image: postgres:13.7
    volumes:
      - ./tmp/db-agrodemi-data:/var/lib/postgresql/data
    container_name: db-prod
    environment:
      POSTGRES_PASSWORD: N0v@cgu@rd
      POSTGRES_USER: novacSystem
    ports:
      - 127.0.0.1:9731:5432
    restart: always

  agrodemi-dev:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: agrodemi-dev
    environment:
      VIRTUAL_HOST: "agrodemi.inspot-technology.com,admservidor.ddns.net,10.0.0.9"
      LETSENCRYPT_HOST: "agrodemi.inspot-technology.com,admservidor.ddns.net"
      LETSENCRYPT_EMAIL: "novacagrodemi@gmail.com"
      ALMACEN: "ADM"
      RAILS_ENV: "production"
      TULU: "N0v@cgu@rd"
      FRAGA: "novacSystem"
      MONTU: "1RzMbNCVAhqkNzH0mTO8f27-kfq1oum6a"
      RAILS_SERVE_STATIC_FILES: "true"
      DISABLE_DATABASE_ENVIRONMENT_CHECK: 1
      API_BASE_URL_DGII_MICROSERVICE: "http://dgii-microservice-prod:9091"
      DGII_CERTIFICATION_DATE: "2025-05-15"
      PORT: "3000"
      RAILS_MAX_THREADS: "5"
      WEB_CONCURRENCY: "2"
    expose:
      - "3000"
    depends_on:
      - db-prod
    restart: always
    command: >
      sh -c "./wait-for-it.sh db-prod:5432 -- sh ./run_server.sh"

  dgii-microservice-prod:
    build:
      context: ./dgii_signature_microservice
      dockerfile: Dockerfile
    container_name: dgii-microservice-prod
    env_file:
      - ./dgii_signature_microservice/.env
    environment:
      ENV: "PROD"
    expose:
      - "9091"
    restart: always
    depends_on:
      - db-prod

  nginx-proxy:
    image: jwilder/nginx-proxy
    container_name: nginx-proxy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
      - ./certs:/etc/nginx/certs
      - ./html:/usr/share/nginx/html
      - ./logs/nginx:/var/log/nginx
    restart: unless-stopped
    depends_on:
      - db-prod

  letsencrypt:
    image: jrcs/letsencrypt-nginx-proxy-companion
    container_name: nginx-letsencrypt
    environment:
      NGINX_PROXY_CONTAINER: nginx-proxy
      DEFAULT_EMAIL: novacagrodemi@gmail.com
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./certs:/etc/nginx/certs
      - ./html:/usr/share/nginx/html
      - ./acme:/etc/acme.sh
    depends_on:
      - nginx-proxy
    restart: unless-stopped

  fail2ban:
    image: crazymax/fail2ban:latest
    container_name: fail2ban
    network_mode: "host"
    cap_add:
      - NET_ADMIN
      - NET_RAW
    volumes:
      - ./fail2ban/data:/data
      - ./logs/nginx:/var/log/nginx:ro  # Leer logs planos
      - /etc/localtime:/etc/localtime:ro
    environment:
      TZ: "America/Santo_Domingo"
    restart: unless-stopped
